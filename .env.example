# Supabase project connection (client-side; the anon key is safe to expose — RLS protects data).
# The service-role key is NEVER used in this app and must never be added here.
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR-ANON-KEY
